#路径
server:
  address: 127.0.0.1
  port: 8080
  servlet:
    context-path: /

# 自定义属性
web:
  path: /usr/local/static

spring:
  application:
    name: zgame-admin

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 100MB